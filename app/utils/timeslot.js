/**
 * Gets the timeslot with the highest discount for selected date.
 *
 * @param {Array.<Object>} availabilityData Data about provider's availailability. See structure in app/utils/transform.js
 * @param {Date} selectedDate Dates to get the best timeslot from.
 * @return {Object} Timeslot data
 */
const getBestTimeSlot = (availabilityData, selectedDate) => {};

export {getBestTimeSlot};
